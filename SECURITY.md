# 安全策略

## 支持的版本

| 版本 | 支持情况 |
| ---- | -------- |
| 0.2.x | ✅ |
| < 0.2 | ❌ |

## 报告漏洞

请**不要**通过公开 Issue 报告安全漏洞。

请使用 GitHub 的私密漏洞报告通道：仓库页 **Security → Report a vulnerability**。
请在报告中尽量附上：影响版本、复现步骤（或最小复现仓库）、影响范围评估。

我们会在 7 天内确认收到，并在修复发布前与你同步披露节奏。

## 已知的安全设计边界

- 引擎不持有任何密钥或后端凭据：数据访问一律经由宿主注入的 `IRecordService` 等服务接口完成；
- 导出通道默认对 CSV 公式注入（`=`, `+`, `-`, `@` 前缀）做防御性转义（`EngineAppearance.safeExport`，docs/20）；
- 示例 Mock 数据仅存在于演示宿主，不包含真实个人信息。
