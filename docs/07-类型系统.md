# 类型系统

所有类型定义在 `src/types/` 下，由 `index.ts` 统一 re-export。

## 1. schema.ts — Schema 类型

```typescript
// 模块 Schema
interface ModuleSchema {
  id: string
  name: string
  version: string
  moduleType: 'list' | 'card' | 'both'
  fields: FieldSchema[]
  permissions: ModulePermissions
  defaultViewMode: 'list' | 'card' | 'create'
  listEditMode?: ListEditMode
  createMode?: 'list' | 'card'
  formulaConfig?: FormulaConfig
  listActions?: ListAction[]
  status: 'active' | 'disabled' | 'error'
}

type ListEditMode =
  | 'inline-dblclick'
  | 'select-then-edit'

// 字段 Schema（核心类型）
interface FieldSchema {
  id: string
  name: string
  key: string
  type: FieldType
  label: string
  required: boolean
  readonly: boolean
  visibleWhen?: Condition
  editableWhen?: Condition
  defaultValue?: unknown
  placeholder?: string
  description?: string
  order: number
  width?: number
  visible: boolean
  fixed?: 'left' | 'right'
  sortable: boolean
  filterable: boolean
  validationRules?: ValidationRule[]
  permission?: FieldPermission
  formula?: FormulaFieldConfig
  dynamicMax?: DynamicMaxConfig
  options?: SelectOption[]
  targetModule?: string          // FK 目标模块
  displayField?: string           // FK 显示字段
  group?: string
  category?: string
  nullableFilter?: boolean        // 允许 null 筛选
  rowAction?: RowActionConfig     // 行内操作
  aggregation?: 'sum' | 'average' | 'count' | 'max' | 'min'
  countCondition?: 'empty' | 'notEmpty'
  treatAsEmpty?: unknown[]        // 视为空的值集合
  prefixStr?: string              // 数字前缀
  suffixStr?: string              // 数字后缀
  decimal?: number                // 数值/百分比/货币的小数位数
  decimalMode?: 'fixed' | 'max' | 'range'  // fixed=补零对齐，max=省略末尾零，range=最少~最多
  maxDecimal?: number             // range 模式时的小数上限
  switchMode?: 'switch' | 'radio' // boolean 展示模式
  trueLabel?: string
  falseLabel?: string
  trueLabelClass?: string
  falseLabelClass?: string
  defaultDateOffset?: number      // 默认日期偏移天数
  relationConfig?: RelationConfig
  reverseRefConfig?: ReverseRefConfig
}
```

### 条件表达式（Condition）

用于实现：
- 有条件显示：`visibleWhen`
- 有条件编辑：`editableWhen`

```ts
type Condition =
  | { and: Condition[] }
  | { or: Condition[] }
  | { not: Condition }
  | {
    left: ConditionValueRef
    operator: ConditionOperator
    right?: ConditionValueRef
  }

type ConditionValueRef =
  | { record: string }  // 从当前记录取值：支持点路径，例如 "currency" / "meta.source.currency"
  | { global: string }  // 从全局上下文取值：支持点路径，例如 "settings.defaultCurrency"
  | { value: unknown }  // 常量值

type ConditionOperator =
  | 'eq' | 'neq'
  | 'gt' | 'gte' | 'lt' | 'lte'
  | 'in' | 'notIn'
  | 'contains' | 'startsWith' | 'endsWith'
  | 'isEmpty' | 'notEmpty'
  | 'exists' | 'notExists'
```

**编写规范**：
- `and/or/not` 可以嵌套组合；`and/or` 的数组为空时分别视为 `true/false`（与 JS every/some 行为一致）
- `record/global` 使用点路径读取值；路径不存在返回 `undefined`（可用 `exists/notExists` 判断）
- `in/notIn` 的 `right` 必须为数组常量（`{ value: [] }`）

**示例**：

```ts
editableWhen: {
  left: { record: 'currency' },
  operator: 'neq',
  right: { value: 'CNY' },
}
```

### FieldType 全部字段类型

| 类别 | 类型 | 说明 |
|------|------|------|
| 基础 | `text`, `number`, `date`, `datetime`, `boolean` | 基本类型 |
| 选择 | `select`, `multi-select`, `fk` | 下拉/外键 |
| 计算 | `formula` | 公式计算字段 |
| 数值 | `percent`, `currency` | 带格式的数字（百分比通过 decimal 控制小数位） |
| 文本 | `phone`, `email`, `url` | 带格式的文本 |
| 媒体 | `image`, `attachment` | 图片/附件 |
| 复杂 | `json` | JSON 对象 |
| 关联 | `one-to-many`, `many-to-many`, `reverse-ref` | 关系字段 |
| 特殊 | `action` | 行操作按钮列 |

### 其他 Schema 相关类型

```typescript
interface SelectOption { label, value, color?, disabled? }
interface ValidationRule { type, value?, message, level }
interface ModulePermissions { view, create, edit, delete, export, configure }
interface FieldPermission { visible, editable, roleBased? }
interface FormulaConfig { enabled, fields: FormulaFieldConfig[], maxDepth, circularDependencyCheck }
interface FormulaFieldConfig { fieldKey, expression, dependencies, resultType, aggregation? }
interface DynamicMaxConfig { enabled, sourceField, ratio, mode, messageTemplate? }
type ListActionType = 'sort' | 'popup-schema' | 'form-submit' | 'custom'
interface ListAction { id, type, label, icon?, order?, target? }
type RowActionType = 'popup-schema' | 'form-submit' | 'export' | 'custom'
interface RowActionConfig { type, label, icon?, target? }
interface ActionTargetConfig { moduleId?, filters?, formFields?, apiEndpoint?, apiMethod?, exportTemplate? }
interface RelationConfig { /* 一对多/多对多配置 */ }
interface ReverseRefConfig { /* 反向引用配置 */ }
interface RelationExtraField { key, label, type, required?, defaultValue?, placeholder?, options? }
```

## 2. record.ts — 记录类型

```typescript
interface RecordEntity {
  id: string
  moduleId: string
  fields: Record<string, unknown>
  version: number
  createdAt: string
  updatedAt: string
  createdBy?: string
  updatedBy?: string
}

interface RecordListResponse { records, total, page, pageSize, hasMore?, cursor? }

interface DraftRecord {
  tempId: string                    // 临时 ID
  fields: Record<string, unknown>
  originalVersion?: number
  validationErrors?: FieldError[]
  isValid: boolean
}

interface FieldError { field, message, level: 'error' | 'warning', code? }

interface CellEditPayload {
  rowId, field, value, oldValue
  mode: 'cell' | 'row' | 'batch'
  source: 'user' | 'formula' | 'lock-fill' | 'quick-create'
}

interface EditContext { originalValue, currentValue, version, fieldKey, recordId }

interface UndoEntry {
  type: 'cell' | 'row' | 'batch'
  recordId, field?, previousValue, timestamp
}

interface RelationEntry {
  id, sourceModuleId, sourceRecordId, targetModuleId, targetRecordId
  fieldKey, extraFields: Record<string, unknown>
  createdAt, updatedAt
}
```

## 3. service.ts — 服务类型

```typescript
// 筛选算子（10种）
type FilterOperator =
  | 'eq' | 'neq' | 'like' | 'notLike'
  | 'in' | 'notIn' | 'between' | 'notBetween'
  | 'isNull' | 'isNotNull'

interface FilterClause {
  field: string
  operator: FilterOperator
  value?: unknown       // 单值
  values?: unknown[]    // 多值（in/notIn/between/notBetween）
}

// 通用 API 响应
interface ApiResponse<T> { success, data: T, message?, errorCode?, details? }

// 列表查询参数
interface ListQueryParams {
  moduleId, filters?: FilterClause[], sort?: SortParam
  page, pageSize, cursor?, viewMode?: 'list' | 'card'
}

interface SortParam { field: string; order: 'asc' | 'desc' }

// 字段补丁参数（乐观锁）
interface PatchFieldParams { moduleId, recordId, field, value, expectedVersion, force? }
interface CreateRecordParams { moduleId, fields: Record<string, unknown> }

// 查询状态
interface QueryState { filters: FilterClause[], sort: SortParam | null, pagination: PaginationState }
interface PaginationState { page, pageSize, total, cursor? }

// 候选值查询
interface CandidateQueryParams { targetModule, keyword?, page, pageSize, filters? }
interface CandidateOption { value: string; label: string; disabled?; data? }
interface CandidateListResponse { options: CandidateOption[]; total; hasMore }

// 错误码
type ErrorCode =
  | 'VERSION_CONFLICT' | 'PERMISSION_DENIED' | 'VALIDATION_FAILED'
  | 'NOT_FOUND' | 'CIRCULAR_DEPENDENCY' | 'FORMULA_ERROR'
  | 'DYNAMIC_MAX_EXCEEDED' | 'MODULE_UNAVAILABLE' | 'SCHEMA_INVALID'
  | 'NETWORK_ERROR' | 'UNKNOWN'

interface StandardError { code: ErrorCode; message; details?; timestamp }
```

## 4. config.ts — 配置类型

```typescript
interface UserViewConfig {
  moduleId, version, columns: ColumnConfig[]
  defaultSort?: SortParam, pageSize
  filterPresets?: FilterPreset[]
  viewMode?: 'list' | 'card'
  cardLayout?: CardLayoutConfig
}

interface ColumnConfig {
  field, width, visible, fixed?: 'left' | 'right', order, sortable
}

interface CardFieldLayout {
  field: string
  span: number          // 占列数（4/5/6/8/16）
  order: number          // 排列顺序
  collapsedByDefault?: boolean
}

interface CardLayoutConfig {
  fields: CardFieldLayout[]
  defaultSpan?: number   // 默认占列数
}

interface FilterPreset { id, name, filters: FilterClause[] }
```

## 5. events.ts — 事件类型

```typescript
interface CellEditEvent extends CellEditPayload { /* 同 CellEditPayload */ }
interface SortChangeEvent { field: string; order: 'asc' | 'desc' }
interface QueryChangeEvent { filters: FilterClause[]; sort: SortParam | null; pagination: { page, pageSize } }
interface PageChangeEvent { page: number; pageSize: number }
interface ViewModeChangeEvent { mode: 'list' | 'card' | 'create'; source: 'user' | 'auto' | 'engine' }
interface QuickCreateEvent { field: string; targetModuleId: string; prefillData?: Record<string, unknown> }
interface DialogResult { action: 'confirm' | 'cancel' | 'close'; payload?: Record<string, unknown> }
interface RequestOpenDialogEvent { dialogType: DialogType; payload: Record<string, unknown> }

type DialogType =
  | 'quick-create' | 'column-settings' | 'formula-detail'
  | 'dynamic-max-confirm' | 'version-conflict' | 'confirm' | 'alert'

interface DraftChangeEvent { drafts: DraftRecord[]; changedIndex?: number }
interface LockColumnEvent { field: string; direction: 'left' | 'right' | 'none' }
```

## 6. index.ts — 统一导出

`src/types/index.ts` 使用 `export type` 语法统一导出所有类型，外部导入只需：

```typescript
import type { ModuleSchema, FieldSchema, RecordEntity, FilterClause, /* ... */ } from '@/types'
```

<!-- BEGIN AUTO:schema-meta-reference -->
### 附录:配置项参考(自动生成)

> 本节由 `scripts/generate-schema-docs.ts` 基于 `src/schemaMeta` 元数据自动生成,请勿手改。
> 重新生成:`pnpm gen:schema-docs`;与 `/playground` 页面共用同一份数据。

#### ModuleSchema 配置项

| 属性 | 适用类型 | 类型 | 默认值 | 生效面 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `id`* | — | 结构性 | — | 结构性 | 模块唯一标识,路由、Schema 存储与记录数据的定位键;创建后不建议变更。 |
| `name`* | — | 字符串 | — | 列表渲染 / 卡片视图 | 模块显示名称,用于引擎标题与模块切换列表。 |
| `version`* | — | 字符串 | — | 结构性 | 语义化版本号;迁移链(migrations)按 fromVersion 升序执行,加载时版本不一致自动迁移并回写。 |
| `moduleType`* | — | 枚举 | — | 列表渲染 / 卡片视图 | 模块提供的视图形态:列表 / 卡片 / 两者;视图切换器按此渲染可选项。 |
| `fields`* | — | 结构性 | — | 列表渲染 / 行内编辑 / 列头筛选 / 表单(新建/编辑) / 卡片视图 | 字段定义数组(FieldSchema[]),在本工具左侧结构树中选择与管理;此处仅在文档中说明,不提供 JSON 编辑。 |
| `permissions`* | — | 对象 | — | 列表渲染 / 行内编辑 / 表单(新建/编辑) / 卡片视图 | 模块级权限 { view, create, edit, delete, export, configure }:view=false 整模块拒绝加载;delete/export 等控制对应操作入口;字段级权限在 FieldSchema.permission 叠加。 |
| `defaultViewMode`* | — | 枚举 | — | 列表渲染 / 卡片视图 | 引擎加载后默认进入的视图。 |
| `listEditMode` | — | 枚举 | — | 行内编辑 / 卡片视图 | 列表视图的编辑交互模式:双击单元格行内编辑,或单击选中后经右上角「编辑」进入卡片编辑。 |
| `createMode` | — | 枚举 | — | 表单(新建/编辑) | 「新增」入口进入的创建形态:表格创建页或卡片创建。 |
| `formulaConfig` | — | 对象 | — | 列表渲染 / 表单(新建/编辑) / 卡片视图 | 公式引擎全局配置 { enabled, fields, maxDepth, circularDependencyCheck };字段级公式在 FieldSchema.formula 声明。 |
| `listActions` | — | 对象 | — | 列表渲染 | 工具栏列表动作数组 { id, type: sort\|popup-schema\|form-submit\|custom\|delete, label, icon?, order?, target? };引擎负责渲染与确认,宿主经 action-trigger 事件执行业务。 |
| `operations` | — | 对象 | — | 列表渲染 | 引擎内置标准操作配置(当前仅 delete):{ delete: { enabled, batch, label, batchLabel, confirmTitle, confirmMessage, batchConfirmTitle, batchConfirmMessage } };启用后渲染行级/批量删除入口(需同时满足 permissions.delete),确认后引擎发标准化事件、由宿主执行。 |
| `migrations`(v2.0) | — | 结构性 | — | 结构性 | 版本迁移数组 { fromVersion, toVersion, migrateRecord, migrateViewConfig?, migrateFilterPresets? },含函数不可 JSON 序列化,仅能在代码中定义;此处仅在文档中说明。 |
| `status`* | — | 枚举 | — | 列表渲染 | 模块状态标记;当前由宿主侧语义消费,引擎核心链路以 permissions.view 为准。 |

#### FieldSchema 配置项(按编辑分组)

##### 基础

| 属性 | 适用类型 | 类型 | 默认值 | 生效面 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `id`* | 全部类型 | 字符串 | — | 结构性 | 字段唯一标识。当前与 key 同值使用,保留独立字段以备分离语义标识与数据键。 |
| `name`* | 全部类型 | 字符串 | — | 结构性 | 字段的内部名称。引擎渲染以 label 为准,name 主要用于检索与运维语义。 |
| `key`* | 全部类型 | 字符串 | — | 结构性 | 记录数据中的字段键(record.fields[key]),列渲染、筛选、排序、编辑保存全部以它定位。重命名历史键请配合 previousKeys 迁移。 |
| `type`* | 全部类型 | 枚举 | text | 列表渲染 / 行内编辑 / 列头筛选 / 表单(新建/编辑) / 卡片视图 | 决定渲染器、编辑器、筛选方式与表单控件的字段类型。切换类型后本面板会按新类型重新过滤适用属性。 |
| `label`* | 全部类型 | 字符串 | — | 列表渲染 / 表单(新建/编辑) / 卡片视图 | 列表头、卡片标签、表单标签显示的标题文本。 |
| `required`* | 全部类型 | 布尔 | false | 表单(新建/编辑) / 卡片视图 | 表单必填标记。当前在快速创建弹窗中强制校验;创建视图以 UI 必填星号提示(校验统一见改进计划批次 D)。 |
| `readonly`* | 全部类型 | 布尔 | false | 行内编辑 / 表单(新建/编辑) / 卡片视图 | 绝对禁止编辑:行内编辑与表单编辑全部禁用,与 editMode=limited 的「仅禁止行内编辑」相区分。 |
| `defaultValue` | 全部类型 | 任意值 | — | 表单(新建/编辑) / 卡片视图 | 新建记录(创建视图/卡片创建)时字段的初始值;日期字段请改用 defaultDateOffset 表达相对今天。 |
| `description` | 全部类型 | 字符串 | — | 表单(新建/编辑) / 卡片视图 | 字段辅助说明文本,当前在卡片创建视图中作为字段提示(field-hint)渲染。 |
| `options` | select / multi-select / status | 选项数组 | — | 列表渲染 / 行内编辑 / 列头筛选 / 表单(新建/编辑) / 卡片视图 | 枚举选项数组 { label, value, color?, disabled? }。color 取语义色调(primary/success/warning/danger/info)或任意 CSS 颜色,渲染为彩色标签;同时作为列头候选值筛选与编辑下拉的数据源。 |
| `group` | 全部类型 | 字符串 | — | 预留 | 预留配置:当前版本引擎未消费(改进计划批次 F 多级表头的规划挂点)。 |
| `category` | 全部类型 | 字符串 | — | 预留 | 预留配置:当前版本引擎未消费,仅作语义分类标注。 |
| `defaultDateOffset` | date / datetime | 数字 | — | 表单(新建/编辑) / 卡片视图 | 新建记录时日期字段的默认值偏移:0=今天,正数=未来,负数=过去。声明后优先于 defaultValue。 |
| `previousKeys`(v2.0) | 全部类型 | 字符串数组 | — | 结构性 | 字段重命名前的旧 key 列表:引擎加载时自动把旧键数据映射到当前 key,配合 SchemaMigration 实现平滑升级。 |

##### 展示

| 属性 | 适用类型 | 类型 | 默认值 | 生效面 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `order`* | 全部类型 | 数字 | — | 列表渲染 / 卡片视图 | 字段在列表与卡片中的显示顺序,从小到大排列。 |
| `width` | 全部类型 | 数字 | — | 列表渲染 | 固定列宽(像素)。未声明时进入弹性列宽通道(minWidth 120,随容器伸缩)。用户可在列设置面板中覆盖。 |
| `visible`* | 全部类型 | 布尔 | true | 列表渲染 / 卡片视图 | 字段默认是否显示;用户可在列设置面板中覆盖,覆盖结果持久化到 UserViewConfig。 |
| `fixed` | 全部类型 | 枚举 | — | 列表渲染 | 将该列固定在表格左侧或右侧(横向滚动时不随动)。用户可在列设置面板中覆盖。 |
| `sortable`* | 全部类型 | 布尔 | false | 列表渲染 | 列头展示排序入口,排序为服务端单列排序(sort 参数随 list 请求下发)。排序状态变更会持久化到 UserViewConfig。 |
| `statusMap` | select / multi-select / status | 对象 | — | 列表渲染 / 卡片视图 | 枚举值 → 颜色的字段级取色表(Record<string,string>);options[].color 优先级更高,未命中时回退 statusMap。 |
| `rowAction` | action | 对象 | — | 列表渲染 | action 字段的行级按钮配置 { type: popup-schema\|form-submit\|export\|delete\|custom, label, icon?, visibleWhen?, danger?, target? };visibleWhen 逐行求值控制按钮显隐,danger 渲染红字。delete 类型由引擎内置渲染(见 ModuleSchema.operations)。 |
| `aggregation` | number / currency / money / percent | 枚举 | — | 列表渲染 | 在表格下方聚合统计条中对该列计算;当前基于客户端、仅统计当前页数据(改进计划批次 C/E 评估全量口径)。 |
| `countCondition` | 全部类型 | 枚举 | — | 列表渲染 | 仅 aggregation=count 时有效:统计空值(empty)或非空(notEmpty)的记录数。 |
| `treatAsEmpty` | 全部类型 | 字符串数组 | — | 列表渲染 | 聚合统计与草稿生命周期中,命中该列表中任意值的记录视同空值(不参与 sum/average 等)。 |
| `prefixStr` | number / currency / money / percent | 字符串 | — | 表单(新建/编辑) | 表单数值编辑器(NumberEditor)内渲染的前缀饰文本,如货币符号。 |
| `suffixStr` | number / currency / money / percent | 字符串 | — | 表单(新建/编辑) | 表单数值编辑器(NumberEditor)内渲染的后缀饰文本;percent 类型自带「%」后缀。 |
| `decimal` | number / currency / money / percent | 数字 | — | 列表渲染 / 行内编辑 / 表单(新建/编辑) / 卡片视图 | 数值/百分比/货币的小数位数:number 无默认,currency 默认 2,percent 默认 0。行内编辑据此做精度校验。 |
| `decimalMode` | number / currency / money / percent | 枚举 | fixed | 列表渲染 / 表单(新建/编辑) / 卡片视图 | fixed=固定位数补零;max=最多 decimal 位、末尾零省略;range=最少 decimal 位、最多 maxDecimal 位。 |
| `maxDecimal` | number / currency / money / percent | 数字 | — | 列表渲染 | 仅 decimalMode=range 时有效:小数位上限。 |
| `switchMode` | boolean | 枚举 | switch | 列表渲染 / 行内编辑 / 卡片视图 | 布尔字段渲染为切换开关(switch)或圆形勾选框(radio)。 |
| `trueLabel` | boolean | 字符串 | 是 | 列表渲染 / 卡片视图 | 布尔字段值为 true 时的显示文本,默认「是」。 |
| `falseLabel` | boolean | 字符串 | 否 | 列表渲染 / 卡片视图 | 布尔字段值为 false 时的显示文本,默认「否」。 |
| `trueLabelClass` | boolean | 字符串 | — | 列表渲染 / 卡片视图 | true 标签的 CSS 类名;不声明时默认绿色(cell-boolean--yes),可传引擎预设 cell-boolean--neutral(灰)或业务自有类。 |
| `falseLabelClass` | boolean | 字符串 | — | 列表渲染 / 卡片视图 | false 标签的 CSS 类名;不声明时默认红色(cell-boolean--no),可传引擎预设 cell-boolean--neutral(灰)或业务自有类。 |

##### 编辑

| 属性 | 适用类型 | 类型 | 默认值 | 生效面 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `placeholder` | 全部类型 | 字符串 | — | 行内编辑 / 表单(新建/编辑) / 卡片视图 | 编辑控件内的占位提示文本。 |
| `validationRules` | 全部类型 | 校验规则数组 | — | 表单(新建/编辑) / 行内编辑 | 校验规则数组(type: required/min/max/minLength/maxLength/pattern/custom,message + level error\|warning)。当前实际生效范围:快速创建弹窗;行内编辑仅数值精度校验,创建视图暂未执行(改进计划批次 D 统一三入口)。 |
| `editMode` | 全部类型 | 枚举 | standard | 行内编辑 | standard=常规(行内+表单);limited=有限编辑(禁止行内编辑,仅创建/专用通道可改)。与 readonly 的绝对禁止相区分。 |
| `quickCreate` | fk | 布尔 | false | 行内编辑 / 表单(新建/编辑) | fk 下拉底部提供「+ 新建」入口:打开弹层按目标模块必选字段快速创建,创建后自动选中;目标无独立模块定义时降级为仅填 name。 |

##### 筛选

| 属性 | 适用类型 | 类型 | 默认值 | 生效面 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `filterable`* | 全部类型 | 布尔 | false | 列头筛选 | 列头展示筛选与排序弹层入口;筛选条件 AND 合并后随 list 请求下发(服务端过滤契约)。 |
| `filterCandidates` | 全部类型 | 布尔 | — | 列头筛选 | Excel 式显式候选值模式。数值/日期字段缺省走关键词/时间段筛选,声明后覆盖为去重候选值列表(datetime 由服务端按天分桶);文本/枚举/外键列无需声明即默认候选值优先。 |
| `nullableFilter` | 全部类型 | 布尔 | — | 列头筛选 | 筛选面板中提供「为空/不为空」三态筛选;筛选栏(SchemaFilterBar)据此展示空值运算。 |
| `highlightStyle` | 全部类型 | 字符串 | — | 列表渲染 / 卡片视图 | 文本包含式筛选命中片段的高亮 CSS 样式,默认黄色背景(如 background:#fff3b0)。 |

##### 条件与权限

| 属性 | 适用类型 | 类型 | 默认值 | 生效面 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `visibleWhen` | 全部类型 | 条件表达式 | — | 列表渲染 / 表单(新建/编辑) / 卡片视图 | 条件表达式,逐行以行数据为 record 上下文求值,控制字段显示。支持 and/or/not 组合与 15 种算子。 |
| `editableWhen` | 全部类型 | 条件表达式 | — | 行内编辑 / 表单(新建/编辑) / 卡片视图 | 条件表达式,求值通过才允许编辑;与 readonly、permission.editable、editMode 叠加生效。 |
| `permission` | 全部类型 | 对象 | — | 列表渲染 / 行内编辑 / 表单(新建/编辑) / 卡片视图 | 字段级权限 { visible, editable, roleBased }。visible=false 整列不渲染;editable=false 禁止行内编辑;roleBased 角色映射已声明、引擎暂未消费(改进计划批次 G)。 |

##### 关系

| 属性 | 适用类型 | 类型 | 默认值 | 生效面 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `targetModule` | fk | 字符串 | — | 列表渲染 / 行内编辑 / 表单(新建/编辑) / 卡片视图 | fk 字段引用的目标模块 ID:候选值经 candidateService 按目标模块拉取,标签解析走 candidateService + getDetail 兜底;quickCreate 快速创建也按目标模块渲染必选字段。 |
| `displayField` | fk | 字符串 | — | 预留 | 预留配置:fk 显示文本字段。当前版本 fk 标签由 candidateService 候选数据与 getDetail 兜底解析决定,引擎未消费该属性(关系字段的显示字段走 relationConfig.displayField)。 |
| `relationConfig` | one-to-many / many-to-many | 对象 | — | 列表渲染 / 行内编辑 / 表单(新建/编辑) / 卡片视图 | 一对多/多对多关联配置 { targetModule, displayField, extraFields? }:渲染可点击的关联摘要,经 relationService 维护关联关系;extraFields 为关联表连带字段(如数量、单价)。 |
| `reverseRefConfig` | reverse-ref | 对象 | — | 列表渲染 / 卡片视图 | 反向引用配置 { sourceModules, relationFieldKey }:展示哪些源模块的记录通过 relationFieldKey 关联字段引用了当前记录(只读统计视图)。 |

##### 公式

| 属性 | 适用类型 | 类型 | 默认值 | 生效面 | 说明 |
| --- | --- | --- | --- | --- | --- |
| `formula` | formula | 对象 | — | 列表渲染 / 卡片视图 | 公式字段配置 { fieldKey, expression, dependencies, resultType, aggregation },expression 为 mathjs 表达式,逐行求值渲染;引擎自带循环依赖检测。 |
| `dynamicMax` | number / currency / money / percent | 对象 | — | 表单(新建/编辑) / 卡片视图 | 数值动态上限 { enabled, sourceField, ratio, mode: hard\|soft, messageTemplate }:以同记录另一字段(surceField×ratio)为上限,hard 拦截提交、soft 仅提示。 |

<!-- END AUTO:schema-meta-reference -->
