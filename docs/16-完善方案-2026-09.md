# 16 - 完善方案（2026-09）

> 基于对仓库整体现状的评估（结构 / 测试 / 文档 / 工程化 / 开源准备），按价值排序给出可执行方案。
> 前提：仓库已开源（github.com/enicn/schemagine，MIT），npm 发布链路（`build:lib` + `prepublishOnly` + exports/types/sideEffects）已齐备。

## 一、现状小结

**已达标**

- src/ 结构清晰：`engine/`、`composables/instanceState.ts`（多实例隔离核心）、`services/api/`（可注入 Service）、`services/mock/`、`editor/`、`styles/tokens.css`；公开入口 `src/index.ts` 干净。
- 双语 README 质量高且基本同步；docs/01-15 覆盖完整。
- TODO/FIXME 为 0，`any` 约 29 处且集中在 vxe-table 边界，可接受。
- type-check 链路已修复（project references + tsBuildInfoFile，`build` 走 `run-p type-check build-only`）。
- dist_backup_*、playwright-report、test-results 均已被 .gitignore 排除。

**主要缺口**

- 无任何 CI（无 .github/workflows）。
- 无 CONTRIBUTING / CHANGELOG / issue 与 PR 模板 / CoC。
- `.trae/` 目录（内部 AI 工作流文档、ps1 脚本）**已被 git 入库**；`src/stores/counter.ts` 为脚手架残留。
- 根目录 `引用指南.md`（29KB 实用内容）、`说明文档.md` 位置不当；docs/11-14 属内部过程文档，开源后易过期。
- e2e 分级脚本依赖 `pwsh scripts/run-e2e-tests.ps1`，Linux/macOS/CI 不可用。
- 单测只覆盖纯函数/服务层（8 个 spec）；instanceState、4 个 Pinia store、useFormula/useMigration/usePermission 等核心无单测。
- 唯一 runtime 依赖 mathjs 仅用了 `evaluate`（`src/composables/useFormula.ts:5`），全量引入包体积偏大。
- e2e 仅 chromium、且跑在 dev server 上。

## 二、完善方案（按批次执行）

### 批次 A：开源形象清理（低风险，先做）

| 项 | 动作 | 验收 |
| --- | --- | --- |
| A1 | 删除 `src/stores/counter.ts`（确认无引用后） | 全局搜索无 import；type-check 通过 |
| A2 | 从 git 中移除 `.trae/`（`git rm -r --cached .trae` 并加入 .gitignore） | 远端不再出现内部工具痕迹 |
| A3 | 本地删除 `dist_backup_*` 四个目录（已 gitignore，纯磁盘清理） | 工作区干净 |
| A4 | 迁移 `引用指南.md` → `docs/17-集成与使用指南.md`；`说明文档.md` 内容并入 README.zh-CN 或 docs/01 后删除根目录文件 | 根目录仅剩 README/LICENSE 等 |

### 批次 B：CI 门禁（价值最高）

新建 `.github/workflows/ci.yml`：

1. job `lint`：`pnpm lint`（去掉 --fix 的校验模式或先 `--fix` 后 `git diff --exit-code`）。
2. job `type-check`：`pnpm type-check`。
3. job `unit`：`pnpm test:unit --run`。
4. job `e2e-p0`：`pnpm playwright test e2e/p0-*.spec.ts`（绕过 pwsh 脚本，见批次 C1）。

Node 20 + pnpm（`cache: pnpm`）。验收：PR 上出现绿色 CI；后续可加 `build:lib` 产物校验 job。

### 批次 C：跨平台与 e2e 强化

1. **C1**：把 `test:e2e:p0/p1/p2/all` 从 pwsh 脚本改为直接调用 playwright（如 `playwright test e2e/p0-engine.spec.ts`），或用 node 重写 `scripts/run-e2e-tests.ts`；保留 ps1 过渡期。CI 与本地共用同一入口。
2. **C2**：e2e webServer 从 `pnpm run dev` 切到 `vite preview`（先 build），更接近真实产物。
3. **C3**（可选）：增加 firefox/webkit 冒烟（p0 级别）。

### 批次 D：核心逻辑单测补齐

优先级从高到低（对应卖点，也最缺回归保护）：

1. `composables/instanceState.ts` — 多实例隔离（createRecordState/createUiState 互不串扰）。
2. 4 个 Pinia store（recordStore / schemaMetaStore / runtimeCacheStore / uiStateStore）— 尤其乐观锁、编辑模式相关状态流转。
3. `useFormula`（当前仅靠 e2e）、`useMigration`、`usePermission`。
4. 组件级测试（@vue/test-utils）从 CreateView 的字段渲染/联动开始，不追求全量。

### 批次 E：依赖与体积

1. mathjs 按需引入：改用 `mathjs/number` ESM 子路径（仅数字求值）或评估 `expr-eval` 等轻量替换；跑 `build:lib` 前后对比产物体积并记录。
2. 校验 `peerDependencies` 范围与 Vue 3.6-beta 的兼容声明（README 已提及，确认 semver 范围准确）。

### 批次 F：社区文件与发版

1. CONTRIBUTING.md（本地开发：pnpm i / dev / test / e2e 分级 / type-check；提交规范沿用现有 conventional commits 中文风格）。
2. CHANGELOG.md（从 v0.1.0 起，可结合 Keep a Changelog 格式）。
3. `.github/ISSUE_TEMPLATE`（bug report + feature request）与 PR 模板（勾选 lint/test 通过）。
4. CODE_OF_CONDUCT.md（Contributor Covenant）。
5. 首次 `npm publish` 前跑 `npm pack --dry-run` 确认 files 只含 dist，体积符合预期。

### 批次 G：文档治理（可与 F 合并）

1. docs/11-14（评估/路线图/Phase 执行指引）标注"已归档/历史文档"或移入 `docs/archive/`；执行指引类文档在 Phase 完成后应标记状态，避免误导。
2. 更新仓库 Wiki（`write_wiki.py` 流程，勿在 UI 重新生成）同步本轮变化。

### 批次 H：lint warning 收敛（✅ 已完成，2026-09-22）

基线 170 条清零，目标提前达成：`lint:check` 现为 **0 errors + 0 warnings** 门禁。各批落地方式：

| 批次 | 规则 | 条数 | 实际收敛方式 | 结果 |
| --- | --- | --- | --- | --- |
| H-a | `playwright/no-wait-for-timeout` | 108 | 逐处审读上下文：约 85% 为「固定延时后紧跟带 timeout 断言/自动等待点击」的死重直接删除；真正需要的改条件等待——引擎加载信号 `.vxe-table` 可见、弹层/表格就绪断言前移、读值改 `toHaveText`/`toHaveClass`（带 timeout）、计数竞态用 `expect.poll`；beforeEach 的 2s 固定延时统一换表格可见信号 | ✅ 升 `error` |
| H-b | `@typescript-eslint/no-explicit-any` | 40 | 三类处理：①测试内树/组行取值 → `Record<string, unknown>` 断言链；②表单模型（formModel/formData）→ `Record<string, unknown>` + EP 组件绑定拆 `:model-value`（模板窄化）/`@update:model-value`；③vxe 事件边界（sort/cell-click/dblclick/dragend/类名钩子）→ **官方 `VxeTableDefines.*EventParams`**（内部消费面 formatter 保持自有结构类型并改可选成员）；调试开关 `(window as any)` → `as unknown as {…}` | ✅ 升 `error` |
| H-c | `playwright/no-conditional-in-test` | 15 | `textContent` 循环收集 → `allTextContents()` + `findIndex/some`（语义不变、少 4 次往返）；拖后腿的条件分支随 H-a 改写消除 | ✅ 升 `error` |
| H-c | `vue/no-deprecated-filter` | 4 | **逐条核实均为误报**：模板内 TS 联合类型断言（`as A \| B`）的竖线被规则误判为 Vue1 过滤器；项目 Vue3-only 无迁移负担 → 规则关停（配置留核实记录） | ⛔ 关停（误报） |
| H-c | `vue/no-mutating-props` | 2 | ActionsConfigPanel 两处 `props.schema.X = val` 改为 `emit('update', { ...props.schema, X })` 新对象上抛（与组件既有 updateSchema 模式同口径） | ✅ 升 `error` |
| H-c | `vue/no-side-effects-in-computed-properties` | 1 | CreateView displayFields computed 内的列配置缺省推导副作用外移为 `watchEffect`（pre 先于渲染，语义等价） | ✅ 升 `error` |

新基线：oxlint 0 + eslint 0 warning + 解耦守卫 + 文档覆盖守卫，全部门禁 0 errors / 0 warnings。

## 三、建议节奏

- **第 1 周**：批次 A + B（清理 + CI 上线）。
- **第 2 周**：批次 C + D 的 1、2 项。
- **后续**：批次 E、F、G 按需穿插，E1 可先量化体积再决定是否替换库。
