# 21 - 成熟度评估与下一步改进计划(2026-09-23)

> 在 docs/16(仓库完善)、docs/19(通用表格引擎 A–I 批次)全部收官、`schemagine/rules` 声明式规则批次(本地 6 提交)完成之际,对项目做一次整体成熟度评估,并给出下一阶段计划。
> 评估基线(2026-09-23 实测):版本 0.3.7(npm 已发布 0.3.0/0.3.7)、单测 506/506(44 文件)、e2e 94/94(1.5m,全量直跑)、type-check 0 错误、lint 0 errors 0 warnings、解耦守卫 + 文档覆盖守卫通过、TODO/FIXME 全库零残留、CI 最近 5 次全绿(但 rules 6 提交未推送,CI 尚未验证)、GitHub 0 star / 0 fork、npm 周下载量近零。

## 一、六维成熟度评估

| # | 维度 | 评级 | 证据(2026-09-23 实测) | 主要缺口 |
| --- | --- | --- | --- | --- |
| 1 | 功能完备度 | ★★★★☆ | docs/19 批次 A–I 全收官:扩展注册表/宿主执行契约/本地数据源/虚拟滚动/校验三入口/组合过滤+保存视图/树形/多级表头/分组小计/i18n 框架/键盘导航/权限消费/xlsx 导入导出/undo-redo/批量事务/实时订阅/运行时诊断;0.3.x 增量:media 四模式、FK 弹窗选择器、行号列、rules 声明式规则(八类规则位+FlowRunner+同构校验) | 远期 backlog 均为条件触发型(打印/SSR/懒加载树形/移动列优先级),无阻断性功能缺口 |
| 2 | 质量保障 | ★★★★¾ | 单测 506/506;e2e 94/94;type-check 0 错误;lint 门禁 0 errors 0 warnings(170→0 收敛后逐规则升 error);双守卫(解耦+文档覆盖)+ schemaMeta 防漏单测;perf benchmark 套件(`pnpm bench`);TODO/FIXME 零残留 | rules 引擎接线仅单测(rulesPipeline/rulesEditor/rules.spec)无 e2e;rules 6 提交未过 CI;e2e webServer 60s 超时在本机不可直跑(见 J3) |
| 3 | 文档 | ★★★★☆ | docs/01–20 体系完整;宿主指南 docs/17 覆盖 props 12/emits 11/FieldType 24/六 Service 方法面且有门禁钉住;双语 README;`pnpm gen:schema-docs` 文档再生成通道;Playground 属性文档 | docs/17 存在死链(引用已不存在的根目录 `说明文档.md`);docs/18 索引漏登 docs/19;深度文档全中文,无英文文档站;仓库 Wiki 滞后(2026-09-11 起) |
| 4 | 发布与分发 | ★★★☆☆ | npm 已发布 0.3.0/0.3.7(此前"未发布"状态已解除);三入口导出(`.` / `./media` / `./rules`);MIT + SECURITY.md + issue/PR 模板齐备;CHANGELOG 规范 | rules 批次(Unreleased)待定版 0.4.0;无文档站、无在线 demo、无示例仓库 |
| 5 | 采用与曝光 | ★☆☆☆☆ | — | 0 star / 0 fork / 下载量近零 / 无外部 issue;与工程质量严重不对称——当前最大瓶颈不在代码 |
| 6 | 技术债 | ★★★★☆ | VxeTableWrapper 已拆六 composables;守卫防回潮;0 TODO | i18n 容器层余量:40 个组件文件仍硬编码中文(G1 备注,locale 仅 60 key);schemaMeta/record/uiState 三个遗留 Pinia store(docs/04 标注);xlsx@0.18.5(npm 版)存在已知 CVE(ReDoS/原型污染,修复版仅在 SheetJS 官方源),当前为 optional peer、仅导入导出消费 |

**结论:引擎本体已达生产级;瓶颈已从"能力建设"转移到"发布收口 + 分发面"。** 后续投入的边际收益排序:把已完成的 rules 批次安全发出去 > 让潜在用户看得见、试得到(文档站/在线 demo/教程) > 继续加深引擎能力。

## 二、批次 J:发布收口(S,立即执行)

| 项 | 动作 | 验收 |
| --- | --- | --- |
| J1 | 推送本地领先的 6 个 rules 提交,确认 CI 全绿(既有教训:本地门禁过而 CI 失败≈没推送) | GitHub Actions 最新 run success;`git status` 与 origin 对齐 |
| J2 | 发版 v0.4.0:Unreleased 三条(rules 契约/子路径包/引擎接线)归档;新增子路径包与 schema 新键均为向后兼容的新能力,按 semver 取 minor(0.3.0 media 首发同例);README/docs/17 版本表述核对;`git tag` + `npm publish`(必要时走蜂窝网络 token 方案,见既有记录) | npm registry 出现 0.4.0;`dist/rules.mjs`/`dist/rules/*.d.ts` 随包发布;CHANGELOG 定版 |
| J3 | 顺手清理三件:① docs/17 §1324 死链改指 `docs/archive/`(根目录 `说明文档.md` 已不存在,且 `.trae` 内部规范不应出现在对外指南);② docs/18 文档索引补登 docs/19 与 docs/21(✅ 已随本文件合入);③ playwright webServer `timeout` 60s→180s(`build && preview` 在 Windows 实测超 60s,e2e 全量本机无法直跑,当前需手动分步 build+preview 绕过;另:本机 `PLAYWRIGHT_BROWSERS_PATH` 环境变量指向文件路径,须覆盖为 `%LOCALAPPDATA%\ms-playwright` 才能起浏览器) | ①③ 合入;`pnpm test:e2e:all` 本机直跑可过 |

## 三、批次 K:质量补强(M)

| 项 | 动作 | 验收 |
| --- | --- | --- |
| K1 | rules 引擎接线 e2e(新增 `e2e/rules.spec.ts` 或并入 batch 系列):行内编辑 compute 链回写(级联+去重)、validate 违规拦截与 force 值重写、`rowAction.action` Effect 产出→`rulesExecutor` 注入消费(未注入时告警丢弃)、`labelWhen` 按行切换按钮标、`aggregate` 统计条追加项;示例模块补 rules 演示位 | 5+ 用例全绿;0.4.0 后首个批次合入,堵住 rules 无浏览器级回归防护的缺口 |
| K2 | i18n 容器层补齐(G1 余量收尾):40 个硬编码中文组件(ListView 工具栏/FilterBar/各对话框/卡片/字段编辑器全集)文案迁移 `t()`;en-US 示例包同步补 key;防回潮——容器组件中文文案改白名单制守卫(可挂 `lint:check`,注释与 aria-label 白名单豁免) | 组件面零硬编码 UI 中文;守卫拦截新增硬编码;切 en-US 演示壳无中文漏出 |
| K3 | xlsx 依赖风险处置:docs/17 导入导出章节补安全说明(npm 版 0.18.5 的已知 CVE、修复版获取渠道、"仅对可信来源文件启用导入"的使用建议);评估 exceljs 替代的结论记入本表(不强制替换) | 文档合入;决策留痕 |

## 四、批次 L:采用面建设(M–L,当前最大杠杆)

> 立项理由:0 star/0 fork 与 506 单测的工程质量完全倒挂。功能再深,没有入口就没人用。本批次不写引擎代码,全部是"看得见、试得到"的建设。

| 项 | 动作 | 验收 |
| --- | --- | --- |
| L1 | 英文文档站:VitePress(Vue 生态同构),以 docs/17(宿主指南)与双 README 为源,英文化组织;部署 GitHub Pages(`enicn.github.io/schemagine`);CI 加 docs build job | 站点上线;README 徽章+链接;核心路径(安装→灌 schema→六个 Service→本地源跑通)全英文可达 |
| L2 | 在线 Playground:演示 app 精简入口(零登录/零后端,`createLocalRecordService` 内存数据)部署 GitHub Pages,预置示例模块与 rules 演示位;README "Try it live" 入口 | 公网可访问;属性文档模式+实时预览可用;与 L1 同域或子路径 |
| L3 | Getting-started 教程:15 分钟路径(新建 Vite 项目→安装→最小 schema→六 Service 最小实现→表格跑通),以独立 `examples/` 目录或配套仓库承载,README Quick Start 与之对齐(当前 Quick Start 缺"从零跑通"完整路径) | 按教程逐条操作可在新环境 15 分钟内得到可交互表格 |
| L4 | 推广动作(发布后执行):GitHub repo topics/description 完善;awesome-vue 提 PR;英文渠道(r/vuejs、dev.to)+中文渠道(vxe-table 社区、掘金、V2EX)各一篇介绍(引擎定位:schema 驱动、六 Service 契约、Playground 在线试);npm keywords 核对(补 table/form/data-grid) | 渠道发布完成;README 附演示 GIF/短视频 |

## 五、里程碑与排序理由

| 顺序 | 批次 | 主题 | 规模 | 依赖 | 说明 |
| --- | --- | --- | --- | --- | --- |
| 1 | J | 发布收口 | S | 无 | 未推送的 6 提交挂着是当前唯一"已完未交"状态;零风险高确定性 |
| 2 | K1 | rules e2e | S | J1(可并行) | 0.4.0 发出去后第一时间补浏览器级防护 |
| 3 | L1 | 英文文档站 | M | J2 | 版本定版后文档站引用稳定;采用面的地基 |
| 4 | L2 | 在线 Playground | S–M | L1 | 与 L1 共用 Pages 部署面;演示 app 已存在,增量是精简与部署 |
| 5 | K2 | i18n 容器层 | M | 无强依赖 | 可穿插;在获得首批国际用户前完成即可 |
| 6 | L3/L4 | 教程与推广 | S | L1/L2 | 有可试的入口之后推广才有效 |

排序理由:J 先行是因为"完成的工作不发出去等于没做";L1/L2 排在 K2 之前,是因为目标用户的获取路径是"看到文档→在线试用→安装",UI 文案暂为中文不阻断这条路径,而 40 文件 i18n 迁移量大、可后置到有真实国际化反馈后更精准;K3 随任一批次顺手带走。

## 六、远期 backlog(维持条件触发,新增两项)

| 项 | 内容 | 升格触发条件 |
| --- | --- | --- |
| 打印/报表 | print CSS 或经 xlsx/PDF 导出承载 | 出现真实报表场景需求 |
| SSR 适配 | Nuxt 环境 CSR-only 限制评估 | 出现 SSR 宿主接入需求 |
| 移动端列优先级 | 窄屏按列优先级隐藏低优先列 | 出现移动端使用反馈 |
| 懒加载树形 | treeConfig 懒加载分支 | 出现深树数据场景 |
| xlsx 替代 | 迁移 exceljs 或引导 SheetJS 官方源 | 宿主报告不可信文件导入场景(K3 文档先行) |
| 仓库 Wiki 更新 | 手工 `write_wiki.py` 再生成(2026-09-11 样式重构后滞后,rules 批次后又一轮滞后) | 任意发版后顺手 |

## 七、验收门禁

沿用 docs/19 §五:每批次合入前 `pnpm type-check` 0 错误、`pnpm lint:check` 0 errors 0 warnings、新增能力带单测或 e2e;涉及契约/类型的批次同步 docs/06、docs/07 与 docs/17(覆盖守卫会拦);样式一律 `--sg-*` token(docs/15)。发布批次(J/L)额外要求:CHANGELOG 定版核对、npm 包 `dist` 三入口完整性、README 双语同步。
