# 22 - 落地执行计划(2026-09)

> docs/21(成熟度评估与批次 J/K/L 立项)的**执行手册**:任务级分解到文件与命令,执行时在「状态」列回填(⬜ 未开始 / ◐ 进行中 / ✅ 完成)。
> 本文只做分解,不含已执行的改动。规模标记:S(≤半天) / M(1–2 天) / L(3 天以上)。
> 通用门禁(每个代码任务合入前,沿用 docs/21 §七):`pnpm type-check` 0 错误、`pnpm lint:check` 0/0、新增能力带单测或 e2e、契约改动同步 docs/06/07/17(覆盖守卫会拦)。

---

## 一、批次 J:发布收口

### J1 推送 rules 批次并验证 CI ⬜ S

| # | 步骤 | 命令/位置 | 验收 |
| --- | --- | --- | --- |
| J1.1 | 推送本地领先 origin 的 6 提交(1c8516c..f99e052) | `git push origin main` | `git status -sb` 不再显示 ahead |
| J1.2 | 等 CI 跑完并确认 | `curl -s https://api.github.com/repos/enicn/schemagine/actions/runs?per_page=1`(本机无 gh) | 最新 run `conclusion: success` |
| J1.3 | (仅当失败)按既有教训排查:本地门禁已全绿(2026-09-23 实测 单测 506/506、e2e 94/94、lint 0/0),CI 失败优先怀疑环境差异(如 e2e webServer 超时——CI 是 Ubuntu,build 快,一般不受 J3.2 的 Windows 超时影响) | 读 actions 日志定位 job | — |

### J2 发版 v0.4.0 ⬜ S(依赖 J1)

对齐 0.3.0 先例(e1ff732:chore(release) 提交 + tag + push + npm publish + 真装验证)。

| # | 步骤 | 命令/位置 | 验收 |
| --- | --- | --- | --- |
| J2.1 | CHANGELOG:`## [Unreleased]` 下三条 Added(rules 契约/rules 子包/引擎接线)归档为 `## [0.4.0] - 2026-09-XX` | `CHANGELOG.md` | Unreleased 段清空,0.4.0 条目完整 |
| J2.2 | 版本号 0.3.7 → 0.4.0 | `package.json` `"version"` | — |
| J2.3 | 版本表述核对:双语 README 与 docs/17 中出现的版本号引用 | `grep -rn "0\.3\." README.md README.zh-CN.md docs/17-*.md` | 引用与 0.4.0 一致或为不带版本号的表述 |
| J2.4 | 构建与包核验 | `pnpm build:lib && npm pack --dry-run` | tarball 含三入口:`dist/schemagine.mjs/cjs`、`dist/media.mjs/cjs`、`dist/rules.mjs/cjs` + `dist/rules/*.d.ts` + `dist/schemagine.css`;files 数与 0.3.7 量级相当 |
| J2.5 | 提交、打标、推送、发布 | `git commit -m "chore(release): v0.4.0"` → `git tag v0.4.0` → `git push origin main --tags` → `npm publish`(token 已在 `~/.npmrc`,registry 端点未被封) | `curl -s https://registry.npmjs.org/schemagine` 的 dist-tags.latest = 0.4.0 |
| J2.6 | 真装验证(可选但建议,0.3.0 先例) | 干净目录 `npm i schemagine@0.4.0`,`node -e "import('schemagine/rules').then(m=>console.log(Object.keys(m)))"` | rules 子路径导出可解析 |

回滚说明:失败的 `npm publish` 不消耗版本号(0.3.0 首发被 403 拦的先例);发布后不可撤回,故 J2.4 的 pack 核验是硬闸。

### J3 顺手清理 ⬜ S(与 J2 同一 PR/提交串带走)

| # | 步骤 | 位置 | 验收 |
| --- | --- | --- | --- |
| J3.1 | 死链修复:删除或改写「参考项目根目录的 `说明文档.md` 和 `.trae/rules/project_rules.md`」一句——根目录 `说明文档.md` 已不存在,且 `.trae` 属内部规范不应出现在对外指南 | `docs/17-集成与使用指南.md:1324` | 文内无对不存在文件的引用;`grep -n "说明文档" docs/17-*.md` 仅剩合理命中 |
| J3.2 | e2e webServer 超时修复:`webServer` 配置块加 `timeout: 180_000`(Windows 本机 build 阶段超 60s,`pnpm test:e2e:all` 无法直跑,2026-09-23 实测) | `playwright.config.ts:40-44` | 本机直跑 `pnpm test:e2e:all` 可过(另需先 `export PLAYWRIGHT_BROWSERS_PATH="$LOCALAPPDATA/ms-playwright"`,见 docs/21 J3 备注) |
| (已完成) | docs/18 索引补登 docs/19 与 docs/21 | `docs/18-维护记录与文档索引.md` | ✅ 已随 docs/21 合入(2026-09-23) |

---

## 二、批次 K:质量补强

### K1 rules 引擎接线 e2e ⬜ M(依赖 J1,可与 J2 并行)

现状:rules 仅有单测(`src/rules/__tests__/rules.spec.ts`、`src/__tests__/rulesPipeline.spec.ts`、`src/__tests__/rulesEditor.spec.ts`),浏览器级零覆盖。演示位:`src/services/mock/sampleSchemas.ts` 的 **module-invoice(发票管理)**——字段级 compute 链(`amount` → watch → `amountWithTax`,`roundTo(amount * 1.13, 2)`,612 行)与模块级 validate(when 违反条件,692 行)。`rowAction.action`/`labelWhen`/`aggregate` **无演示位**,且演示壳(src/views/)未注入 `rulesExecutor`(仅 SchemaEngine 声明了 prop,`src/engine/entry/SchemaEngine.vue:54`)。

| # | 任务 | 改动点 | 验收 |
| --- | --- | --- | --- |
| K1.1 | 新增 `e2e/rules.spec.ts`,首期两用例(现有演示位直测,写法沿用 batch-h 的文件内 helper 约定:`page.goto('/module/module-invoice')` + 动态探测列序):**R1.1** 行内编辑改 `amount` 确认后 `amountWithTax` 同步为 roundTo(amount×1.13,2);**R1.2** 构造违反模块级 when 的行内编辑,确认被拦截且出现提示 | 新文件 `e2e/rules.spec.ts` | 两用例绿;进 CI 的 `test:e2e:ext` 清单(package.json scripts 与 ci.yml e2e-extended job 的文件列表都要加) |
| K1.2 | 补 `rowAction.action` 演示位 + 演示壳注入最小 executor:module-invoice 加一条带 `action`(如 invoke 类 Effect)的行动作;演示壳(挂 SchemaEngine 的视图组件)传 `:rules-executor` 把 Effect 转 console/ElMessage 展示 | `src/services/mock/sampleSchemas.ts`(module-invoice)、演示壳挂载点(`src/views/` 下使用 SchemaEngine 的组件) | **R1.3** 点击动作行按钮出现 Effect 结果;未注入路径的告警丢弃语义已有单测,不重复 e2e |
| K1.3 | (可选,随 K1.2 顺带)`labelWhen`/`aggregate` 演示位与用例 R1.4/R1.5:labelWhen 按行切换按钮文案;aggregate 在统计条追加 sum 项 | 同上 | 用例绿;零规则模块(module-voucher 等)统计条不出现 rules 项(零开销语义) |

### K2 i18n 容器层补齐(G1 余量收尾) ⬜ M-L

现状:40 个组件文件硬编码中文(容器/编辑器/卡片全集),locale 基准包仅 60 key(`src/locales/zh-CN.ts`,按域分组点路径:`table.filter.*` 等)。

| # | 任务 | 改动点 | 验收 |
| --- | --- | --- | --- |
| K2.1 | 分组迁移(顺序按用户可见度):①容器动作面(ListView 工具栏/ListActionBar/SchemaFilterBar/各对话框) ②字段编辑器(`src/components/field/editors/*`、FieldEditorFactory) ③卡片(`src/components/card/*`) ④其余(MediaImageCell/编辑器面板等)。文案迁 `t('域.key')`,新命名空间沿用既有风格:`list.* / dialog.* / form.* / card.* / editor.* / media.*` | 上述 40 文件 + `src/locales/zh-CN.ts`(新增 key)+ `src/locales/` 演示壳 en-US 示例包同步补 key | 每组迁完跑 `pnpm test:e2e:all`(G1.1/G1.2 既有 i18n 用例不回归)+ 手测 `?locale=en-US` 抽查该组界面无中文漏出 |
| K2.2 | 防回潮守卫:新脚本扫描 `.vue` 模板插值与 `.ts` 字符串字面量中的 CJK 字符,白名单制豁免(注释、aria-label、sampleSchemas/演示数据、schemaMeta 描述、locales 本身、`__tests__`),接入 `lint:check` | 新文件 `scripts/check-i18n-hardcode.mjs` + `package.json` lint:check 串 + 白名单配置 | 守卫过;人为加一条硬编码能被拦(自测一次) |
| K2.3 | README 双语 i18n 特性行更新(声明 UI 文案全量可切换) | `README.md`、`README.zh-CN.md`(平行更新) | 两文件同步 |

注意:迁移只动文案字符串,不改逻辑;遇 `showMessage`/`ElMessage` 直拼中文一并走 `t()`。估算:40 文件 × 平均 3–6 条文案,建议按 K2.1 的四组各成一个提交,任何一组可独立中止不破坏中间态。

### K3 xlsx 依赖风险文档化 ⬜ S

| # | 任务 | 改动点 | 验收 |
| --- | --- | --- | --- |
| K3.1 | docs/17 新增小节「导入导出安全说明」(建议挂 §8 扩展与进阶下,如 §8.6):npm 版 xlsx@0.18.5 存在已知 CVE(ReDoS CVE-2023-30533、原型污染类,修复版仅经 SheetJS 官方源分发);本引擎仅将其用于文件解析/生成,不执行其中公式,导出侧已有公式注入防护;使用建议——仅对可信来源文件启用导入、或宿主自行 pin 官方源版本;exceljs 替代评估结论留痕(暂不替换) | `docs/17-集成与使用指南.md` §8 + §1.2 对等依赖表 xlsx 行(46 行)补一句「详见 §8.x 安全说明」 | 文档覆盖守卫不受影响(xlsx 非守卫面);CHANGELOG 记 docs 条目 |

---

## 三、批次 L:采用面建设

### L1 英文文档站(VitePress) ⬜ M,分三阶段(依赖 J2)

| # | 任务 | 改动点 | 验收 |
| --- | --- | --- | --- |
| L1.1 | 站点骨架:新目录 `docs-site/`(VitePress 1.x,devDependency),`.gitignore` 加 `docs-site/dist`;首页 + 侧栏骨架;`pnpm docs:dev/docs:build` 脚本 | `docs-site/`、`package.json` scripts、CI 保留(站点不进 npm 包,`files: ["dist"]` 不受影响) | `pnpm docs:build` 产出静态站 |
| L1.2 | 首期内容(英文,P1):Intro(定位:六 Service 契约 + schema 驱动 + 三视图)/ Installation(对齐 docs/17 §1 含去重配置警示)/ Quick Start(对齐 docs/17 §2 四步)/ Services(六 Service 契约速览,链接深文档) | `docs-site/guide/*.md` | 英文母语可读性自检;与 docs/17 事实一致(版本、方法签名) |
| L1.3 | 部署:GitHub Actions 新 workflow `docs.yml`——push 到 main 且 `docs-site/**` 变更时 build + 部署 Pages;`base: '/schemagine/'`;Pages 分支/flow 按 repo 现设置(actions 配置一次) | 新文件 `.github/workflows/docs.yml`、`docs-site/.vitepress/config.ts` | `https://enicn.github.io/schemagine/` 可访问;README 徽章 + 链接(双语平行) |
| L1.4 | (P2,后续)宿主指南全量英文化 + (P3)API 参考由 schemaMeta 自动生成对接 `pnpm gen:schema-docs` 产物 | — | 触发条件:首批真实用户反馈后 |

### L2 Playground 在线 demo ⬜ M(依赖 L1.3 部署通道)

| # | 任务 | 改动点 | 验收 |
| --- | --- | --- | --- |
| L2.1 | 演示 app 增加「demo 模式」入口页(或精简路由):直达 `/playground`,预置示例模块选择;playground 本身已是内存本地源(批次 C1,不写 localStorage),可公网安全运行 | `src/router/index.ts`(可选 query/路径守卫)、演示壳入口页 | 无登录/无后端即可交互 |
| L2.2 | 构建 base 适配:`vite build --base=/schemagine/demo/`(或独立 config),与 L1 同 workflow 部署到子路径 | `docs.yml` 扩展 or 演示 build 步骤 | 公网 `/schemagine/demo/playground` 属性文档 + 实时预览可用;rules 演示位(module-invoice compute 链)可试 |
| L2.3 | README 双语 "Try it live" 入口 + 站点首页互链 | README ×2、docs-site 首页 | 链接可达 |

### L3 Getting-started 教程与示例工程 ⬜ M(依赖 J2)

| # | 任务 | 改动点 | 验收 |
| --- | --- | --- | --- |
| L3.1 | 最小示例工程 `examples/minimal-vite/`:Vite + Vue 3 + schemagine,`createLocalRecordService` 喂数组,单模块 schema(6–8 字段覆盖常用类型),README 逐步说明 | 新目录 `examples/`(确认不进 npm 包 files 白名单) | 新环境 `pnpm i && pnpm dev` 得到可交互表格;目录自带最小 e2e 可选 |
| L3.2 | 教程文:15 分钟路径(安装→六 Service 最小实现→切 HTTP mock→上生产),发布到文档站(L1)与 README Quick Start 链接对齐(现有 Quick Start 缺"从零跑通"完整闭环,补齐不推翻) | docs-site + README ×2 | 逐步照做可复现 |

### L4 推广动作 ⬜ S(依赖 L1.3/L2.2 上线后)

| # | 动作 | 内容要点 |
| --- | --- | --- |
| L4.1 | GitHub 仓库元数据 | topics 补全:`vue vue3 schema dynamic-form data-table form-builder crud low-code vxe-table element-plus`;description 与首页标语核对;社交预览图可用现 brand 图标 |
| L4.2 | awesome-vue PR | Tables/Forms 分区;条目一句话 + 站点/demo 链接;被合并前不宣传"listed in awesome-vue" |
| L4.3 | 英文帖(1 篇) | r/vuejs 或 dev.to:痛点(schema 驱动后台表格)、六 Service 契约一张图、在线 Playground 试玩链接、与 vxe-table/Element Plus 的关系(基于而非替代) |
| L4.4 | 中文帖(1 篇) | vxe-table 社区(gitee)/掘金/V2EX 择一首发:同结构,强调 vxe-table 生态的 schema 层封装 |
| L4.5 | README 增强 | 顶部演示 GIF/30s 短视频(表格交互+行内编辑+Playground 反查),双语平行 |

---

## 四、执行顺序总表

| 序 | 任务 | 规模 | 依赖 | 产出节点 |
| --- | --- | --- | --- | --- |
| 1 | J1 推送 + CI 验证 | S | 无 | rules 批次进入 origin |
| 2 | J3.1/J3.2 清理 | S | 无(建议随 J2 一起提交) | docs/17 净化 + e2e 本机直跑恢复 |
| 3 | J2 发版 0.4.0 | S | J1 | npm latest = 0.4.0 |
| 4 | K1 rules e2e | M | J1 | e2e 94→97±,rules 有浏览器级防护 |
| 5 | L1 文档站(骨架+首期+部署) | M | J2 | 公网文档站上线 |
| 6 | L2 Playground 在线 | M | L1.3 | 公网 demo 上线 |
| 7 | L3 示例工程与教程 | M | J2 | examples/ + 教程 |
| 8 | L4 推广 | S | L1/L2 | 渠道发布 |
| 9 | K2 i18n 容器层 | M-L | 无强依赖(建议排在 L1 后,拿到真实用户前完成) | 40 文案面可切换 + 守卫 |
| 10 | K3 xlsx 文档 | S | 无 | 随任意批次带走 |

关键路径:J1 → J2 → L1 → L2 → L4(约 5–7 个工作日到"可推广"状态);K1 并行插入 J2 后;K2 独立泳道。

## 五、风险与回滚

- **J2 发布不可逆**:pack 核验(J2.4)是硬闸;token 失效则回到蜂窝网络方案(既有记录),不阻塞 J1/J3。
- **L1/L2 Pages 子路径**:`base` 配置错误是静态站 404 首因;部署后逐链抽测,回滚 = workflow 还原(站点不影响库本体)。
- **K2 迁移面广**:按四组独立提交,任何一组可中止;守卫脚本先灰度(先 warn 后 error)一个提交周期。
- **K1 演示位改动会进发布包的示例数据**:sampleSchemas 属 dev app(mock),不进 dist,无发版影响。
- 全程不触碰 `src/engine` 主链路(J/K/L 唯一引擎侧改动是 K1.2 的演示壳注入,属演示 app 层)。

## 六、门禁与状态回填约定

- 代码任务:docs/21 §七门禁全项;e2e 新文件同步 `package.json` scripts 与 ci.yml 的文件清单(0.3.0 补 e2e-extended job 的同款注意点)。
- 发布/部署任务:CHANGELOG 定版核对 + dist 三入口核验 + README 双语同步。
- 每完成一项,回填本文「状态」列并在 docs/18 维护记录加一行(日期 + 任务号 + 一句话)。
